import React from "react";
const CartItemsContext = React.createContext([]);
export const QtyCardContext = React.createContext([0]);

export default CartItemsContext;

// export default  QtyCardContext;
